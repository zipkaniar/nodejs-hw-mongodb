get:
  tags:
    - Contacts
  summary: Tüm iletişimleri getirir
  operationId: getContacts
  description: Kullanıcının tüm iletişim bilgilerini döndürür.
  security:
    - bearerAuth: []
  parameters:
    - name: page
      in: query
      required: false
      schema:
        type: integer
    - name: perPage
      in: query
      required: false
      schema:
        type: integer
  responses:
    '200':
      description: Başarılı yanıt.
    '401':
      description: Yetkilendirme hatası.
